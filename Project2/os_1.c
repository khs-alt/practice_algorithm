#include <stdio.h>

int main() {
	int process_num, process_id;
	int arrival_time, service_time;
	int buffer[1024];
		
	//FILE* fp;
	//fp = fopen("process_list_5.txt", "r");

	//for(int i = 0; i < process_num; i++)
	//	fread(buffer, sizeof(int), 1024, fp);
	

}